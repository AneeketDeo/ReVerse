using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class PortalEntered : MonoBehaviour
{
    /*[SerializeField]
    private followPlayer followplayerscript;

    [SerializeField]
    private Animator animator;

    [SerializeField]
    private GameObject reverseTransitionPanel;

    private bool count = true;

    void Start() {
        // followplayerscript = GameObject.GetComponent<followPlayer>();
    }
    private void OnTriggerEnter2D(Collider2D collider) {
        followplayerscript.enabled = false;
        Run_ReverseAnimation();
    }

    public void Run_ReverseAnimation(){
        if(count){
            PanelTrigger(true);
            animator.SetTrigger("Reverse_FrontWipe");
            Invoke("PanelTriggerFalse", 5.5f);
            count = false;
        }
        
    }

    public void PanelTrigger(bool state){
        reverseTransitionPanel.SetActive(state);
    }

    public void PanelTriggerFalse(){
        reverseTransitionPanel.SetActive(false);
    }*/

    
}
